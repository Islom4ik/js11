let a = +prompt('Введите число');
let b = +prompt('Введите второе число');
let perPrym = (a + b) *2;
let ploshKvad = (a * b);

function out(a,b) {
    if (a == b) {
        alert(perPrym)
    } else (a != b) 
        alert(ploshKvad)
}

out(a, b)
